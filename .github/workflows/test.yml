name: Test
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  supervisor-and-lifecycle:
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: stack
      - run: cargo test --locked --manifest-path stack/Cargo.toml
      - run: node --check electron/main.js
      - name: Build the pinned Rust dependency
        timeout-minutes: 12
        shell: bash
        run: bash scripts/build-remoteclient.sh
      - name: Test lifecycle and the real process protocol
        timeout-minutes: 5
        shell: bash
        run: |
          binary_path="$(node -p 'require("node:path").resolve("bin/robrowser-remoteclient" + (process.platform === "win32" ? ".exe" : ""))')"
          REMOTECLIENT_BIN="$binary_path" node --test --test-timeout=45000 --test-reporter=spec tests/*.test.cjs
