# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
.env
.env.local

# Rust / Tauri
target/
src-tauri/target/
src-tauri/gen/

# Build output
dist/
build/
*.app
*.dmg

# Vendored upstreams (fetched by scripts/bootstrap.sh, never committed)
/vendor/

# Runtime state: generated conf, schema copies, endpoint discovery, settings
/.ragnarokmac/

# Game assets — copyrighted by Gravity Co., Ltd. NEVER commit these.
*.grf
*.gpf
resources/
assets/client/
DATA.INI
data.ini

# Runtime state
.ragnarokmac/
*.sqlite
*.log

# Client folders — copyrighted game assets, same rule as *.grf.
# Root-anchored so they cannot shadow a legitimate src/data/ later.
/BGM/
/System/
/AI/
/dll_exe/
/data/
/client/

# Packaging output and vendored binaries (rebuilt by scripts/package.sh)
/payload/
/bin/
/vendor/node/
